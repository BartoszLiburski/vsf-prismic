{
  "name": "@vue-storefront/extension-cms-prismic",
  "version": "0.1.0",
  "description": "Extension to display content from headless CMS Prismic, now using Elastic Search",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "publishConfig": {
    "access": "public"
  }
}
